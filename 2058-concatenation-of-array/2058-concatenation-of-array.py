class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:   
        res=[]

        for val in nums:
            res.append(val)
        for val in nums:
            res.append(val)
        return res

        