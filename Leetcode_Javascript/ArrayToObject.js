let name_arr = ["yonas", "amani", "lidia"];

let obj = new Map(name_arr.map((name, index) => [index, name]));

console.log(obj);
/* {
    0 : 'yonas',
    1 : 'amani',
    2 : 'lidia' 
    } 
*/
// Elias Kibret
