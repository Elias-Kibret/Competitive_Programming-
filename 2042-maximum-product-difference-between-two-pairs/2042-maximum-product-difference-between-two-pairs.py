sys.stdout = open('user.out', 'w')
for nums in map(loads, sys.stdin):
    a = max(nums)
    nums.remove(a)
    aa = max(nums)
    b = min(nums)
    nums.remove(b)
    bb = min(nums)
    nums.clear()
    result = (a * aa) - (b * bb)
    print(dumps(result).replace(' ', ''))
exit()